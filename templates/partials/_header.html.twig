<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Navbar</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav me-auto" >
                <li class="nav-item">
                    <a class="nav-link active" href={{ path('home.index') }}>Accueil
                        <span class="visually-hidden">(current)</span>
                    </a>
                </li>

                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href={{ path('ingredient.index') }}>Ingrédients
                            <span class="visually-hidden">(current)</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href={{ path('recipe.index') }}>Recettes </a>
                    </li>
                {% endif %}
            </ul>
            <div>
                <ul class="navbar-nav me-auto">
                {% if app.user %}
                        <li class="nav-item dropdown" style="margin-right: 4rem;">
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.fullName }}</a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('user.edit.password',{id : app.user.id}) }}">Modifier votre mot de passe</a>
                                <a class="dropdown-item" href="{{ path('user.edit', {id : app.user.id}) }}">Modifier votre profil</a>
                                <div class="dropdown-divider"></div>

                                <a class="dropdown-item" href="{{ path('security.logout') }}">Se deconnecter</a>
                            </div>
                        </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link active" href={{ path('security.signup') }}>S'inscrire
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href={{ path('security.login') }}>Connexion</a>
                    </li>

                {% endif %}

                </ul>


            </div>
        </div>
    </div>
</nav>